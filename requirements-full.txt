# Full research-pipeline dependencies — install on your OWN machine
# (needs internet; a fresh venv with Python 3.10 or 3.11 is recommended —
# some of these, especially shap's llvmlite dependency, don't yet support
# the newest Python releases).
#
#   python3.11 -m venv venv
#   source venv/bin/activate   (or venv\Scripts\activate on Windows)
#   pip install -r requirements-full.txt

numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
networkx>=3.1
matplotlib>=3.8
pyyaml>=6.0
requests>=2.31

# Causal inference / discovery
dowhy>=0.8            # 0.11 does not exist on PyPI yet — 0.8 is latest as of writing, re-check before pinning
causal-learn>=0.1.3.8

# Explainability — needs llvmlite, which needs Python <=3.12 as of writing
shap>=0.45

# RL weighting
gymnasium>=0.29
stable-baselines3>=2.3

# Dashboard (also listed in requirements.txt for Streamlit Cloud)
streamlit>=1.35
plotly>=5.20
