# Full research pipeline deps (causal discovery, causal inference, RL
# weighting, explainability, and the Streamlit dashboard).
# Kept as one file so `python run_pipeline.py` and the dashboard both work
# from a single install.

streamlit>=1.35
pandas>=2.0
numpy
scipy
scikit-learn
networkx>=3.3
matplotlib
plotly>=5.20
pillow
pyarrow
shap
lime
graphviz
pydot
